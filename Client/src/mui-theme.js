import { createTheme } from "@mui/material";



export const theme = createTheme({
    palette: {
      type: 'light',
      primary: {
        main: '#ccb5f1',
        contrastText: "#000000",

      },
      secondary: {
        main: '#a28dc7',
      }

    }
  });